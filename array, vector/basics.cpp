#include<bits/stdc++.h>
using namespace std;
int main(){
  // cout<<"kire"<<endl<<"hatt\n"; // yeh nya line me shift karta ha complier ko
  // int appleCow=4;    // camel case variable naming
  // int appleboy=5;   //lowercase variable naming
  // int APPLE=appleCow+appleboy; // upper case naming
  // cout<<APPLE;
  // float x=4.5;
  // cout<<x;

  // int x;
  // cin>>x; // taking input of x
  // cout<<"the value of x is: "<<x;  // giving output value

// addition of two numbers
  
  // int a;
  // cout<<"The value of a is: ";
  // cin>>a;
  // int b;
  // cout<<"The value of b is: ";
  // cin>>b;
  // int c=a+b;
  // cout<<"the sum of a nd b is: "<<c;


  // swapping of two numbers
  // int a,b;
  // cin>>a>>b;
  // int temp=a;
  // a=b;
  // b=temp;
  // cout<<"the value of a is: "<<a<<endl;
  // cout<<"The value of b is: "<<b<<endl;

  // swapping two numbers without using extra variables
  // int a,b;
  // cin>>a>>b;
  // a=a+b;
  // b=a-b;
  // a=a-b;
  // cout<<"The value of a is: "<<a<<endl<<"The value of b is: "<<b<<endl;
  // cout<<a+b;

  // logical operator
  
  // bool a=true;
  // bool b=false;
  // cout<<!a;
  // cout<<endl<<!b;

  // bitwise operator
  
  // cout<<(5<<2)<<endl;  // left shift operator multipled by 2^n
  // cout<<(14>>3)<<endl;  // right shift operator divided by 2^n
  // cout<<(4&2)<<endl;       // bitwise and operator
  // cout<<(4|2)<<endl;     // bitwise or operator
  // cout<<(4^2)<<endl; // bitwise xor operator


  // size of operateter
  // int a;
  // cout<<sizeof(a);   // gives the size of a in bytes

  // ternary operator
  // int a=5;
  // int b=6;
  // (a!=b)? cout<<"a is not equal to b": cout<<"a is equal to b";

  // casting opertor
  
  // float a=6.7;
  // //cout<<int(a);
  // int c=(int)a;
  // cout<<c;


  // gives the address of operator
  // float a=6.7;
  // cout<<&a;


  // USE OF A SWITCH CASE 
// char ch;
//   cin>>ch;
//   switch(ch){
//     case 'A':{
//       cout<<"vowel: "<<ch;
//       break;
//     }
//     case 'E':{
//       cout<<"Vowel: "<<ch;
//       break;
//     }
//     case 'I':{
//       cout<<"Vowel: "<<ch;
//       break;
//     }
//     case 'O':{
//       cout<<"Vowel: "<<ch;
//       break;
//     }
//     case 'U':{
//       cout<<"Vowel: "<<ch;
//       break;
//     }
//     default:{
//       cout<<"consonent: "<<ch;
//       break;
//     }
    
//   }

// method to find modulo of a number both for positive and negative numbers
  // int a=-1;
  // int k=5;
  // int mod=((a%k)+k)%k; // this method works for both positive and negative numbers
  // cout<<mod;
  
  

  return 0;
}